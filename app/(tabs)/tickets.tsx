import { View, Text } from 'react-native';

export default function Tickets() {
  return (
    <View style={{ flex:1, alignItems:'center', justifyContent:'center' }}>
      <Text style={{ color:'#fff' }}>Tickets (coming soon)</Text>
    </View>
  );
}